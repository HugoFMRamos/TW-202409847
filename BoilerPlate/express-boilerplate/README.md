# Express Boilerplate (Users + Auth)

## Prerequisites
- Node 20+
- MongoDB running locally or a cloud URI

## Setup
```bash
cp .env.example .env
# edit .env (MONGO_URI, JWT_SECRET)
npm install
npm run dev
